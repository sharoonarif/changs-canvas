// const drawPencil = () => {
// 	const context = getContextById('changs-other-canvas');
// 	context.clearRect(0, 0, width, height);	
// 	context.lineWidth = 1;

// 	drawLineTo(context, { x: 10, y: 10 }, { x: 100, y: 100 });
// };